python -m pip install --upgrade pip setuptools wheel
python -m pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
python -m pip install -r requirements.txt

python -c "import torch; print(torch.__version__, torch.version.cuda, torch.cuda.is_available())"
python initialization.py --task all --ckpt-dir /srv/scratch/speechdata/SAPC_Team/meanVC_checkpoint

gcc need to load for fairseq, cython also need to fix